{
  "name": "smart-sleep-economy-server",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:remote": "wrangler d1 migrations apply DB --remote"
  },
  "dependencies": {
    "viem": "2.56.3"
  },
  "devDependencies": {
    "solc": "0.8.24",
    "wrangler": "4.129.0"
  }
}
